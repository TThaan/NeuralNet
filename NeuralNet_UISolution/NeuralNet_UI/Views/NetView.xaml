<UserControl x:Class="NeuralNet_UI.Views.NetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NeuralNet_UI.Views"
             xmlns:sd="clr-namespace:NeuralNet_UI.SampleDataViewModels"
             xmlns:conv="clr-namespace:NeuralNet_UI.Converters"
             mc:Ignorable="d">
    
    <d:UserControl.DataContext>
        <sd:NetVMSampleData/>
    </d:UserControl.DataContext>

    <UserControl.Resources>
        <conv:TestConverter x:Key="TestConverter"/>
        <conv:DataToCanvasConverter x:Key="DataToCanvasConverter"/>
    </UserControl.Resources>

    <!--Width="1320" Height="495"-->
    <Border Name="thisRootElement" Background="AliceBlue">
        <Viewbox>
            <ContentControl Content="{Binding ThisNetVM, Converter={StaticResource DataToCanvasConverter}}"/>
        </Viewbox>
    </Border>
</UserControl>
